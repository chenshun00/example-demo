spring:
  cloud:
    consul:
      enabled: true
      host: localhost
      port: 8500
      discovery:
        register: false
        health-check-interval: 10s # consul 健康检查频率
    gateway:
      discovery:
        locator:
          enabled: true
  application:
    name: gateway
