spring:
  cloud:
    nacos:
      discovery:
        register-enabled: true
        heart-beat-interval: 5000
        heart-beat-timeout: 5000
        server-addr: 127.0.0.1:8848
        group: Base
        namespace: test
        enabled: true
    gateway:
      discovery:
        locator:
          enabled: true
  application:
    name: gateway
